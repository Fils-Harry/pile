ifig, ax = plt.subplots(figsize=(10, 8))
plot_importance(best_model, ax=ax, height=0.5) # Ajustez la hauteur si nécessaire

# Personnalisez le graphique pour une meilleure lisibilité
ax.set_title('Feature Importance', fontsize=20)
ax.set_xlabel('F score', fontsize=16)
ax.set_ylabel('Features', fontsize=16)
ax.tick_params(axis='y', labelsize=12)  # Ajustez la taille des étiquettes de l'axe y
ax.tick_params(axis='x', labelsize=12)  # Ajustez la taille des étiquettes de l'axe x

# Affichez le graphique
plt.show()

